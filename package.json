{
  "name": "web-component-dropdown",
  "version": "1.0.0",
  "description": "Web Component Demos",
  "scripts": {
    "build": "tsc -p tsconfig.json --outDir src",
    "start": "web-dev-server --app-index src/button-demo.html --node-resolve --open",
    "watch": "tsc -p tsconfig.json --outDir src --watch"
  },
  "dependencies": {
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "@web/dev-server": "^0.4.3",
    "typescript": "^5.3.3"
  }
}
